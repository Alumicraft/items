[project]
name = "items"
version = "0.1.0"
description = "Item master import manager for ERPNext"
requires-python = ">=3.10"

[build-system]
requires = ["flit_core>=3.4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "items"
